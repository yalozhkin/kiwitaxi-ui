mixin place-routes
  .place
    a(href= path.root+menu.place.url)
      h4= place.name
      p 25 destinations

mixin place-offers
  .place
    a(href= path.root+menu.place.url)
      h4= place.name
      p= offer.price
