figure.testimonial
  blockquote.testimonial_content
    = review.content
    p= review.score
  footer.testimonial_details
    cite= client.name
    ul.inline
      li= place.name+" → "+place.name
      li= booking.pickup.date
      li= vehicle.name
